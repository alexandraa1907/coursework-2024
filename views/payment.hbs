<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" type="text/css">
    <title>Lex</title>
</head>

<body>
    <div>
        <h2>
            <a href="/">На головну</a>
        </h2>
    </div>
    <div>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Дата консультації</th>
                    <th>Клієнт</th>
                    <th>Юрист</th>
                    <th>Спеціалізація</th>
                    <th>Хто зареєстрував</th>
                    <th>Ціна</th>
                    <th>Стан оплати</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each payments}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.date}}</td>
                    <td>{{this.c_name}}</td>
                    <td>{{this.l_name}}</td>
                    <td>{{this.s_name}}</td>
                    <td>{{this.w_name}}</td>
                    <td>{{this.price}}</td>
                    <td>{{this.is_paid}}</td>
                    <td>
                        {{#unless this.is_paid}}
                        <form action="/pay/{{this.id}}" method="post">
                            <button type="submit">Провести оплату</button>
                        </form>
                        {{/unless}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</body>

</html>